import java.util.*;

public class un extends Deplacement {
	 
	ArrayList list = new ArrayList();
	
	public un(){
		//melange();
	 }
	
/*	public void uneListe(){
		for(int i=0; i<52; i++){
			if(i<defausse.size())list.add(defausse.get(i));
			if(i<C1.size()+defausse.size())list.add(C1.get(i));
			if(i<C1.size()+defausse.size()+C2.size())list.add(C2.get(i));
			if(i<C1.size()+defausse.size()+C2.size()+C3.size())list.add(C3.get(i));
			if(i<C1.size()+defausse.size()+C2.size()+C3.size()+C4.size())list.add(C4.get(i));
			if(i<C1.size()+defausse.size()+C2.size()+C3.size()+C4.size()+C5.size())list.add(C4.get(i));
			if(i<C1.size()+defausse.size()+C2.size()+C3.size()+C4.size()+C5.size()+C6.size())list.add(C6.get(i));
			if(i<C1.size()+defausse.size()+C2.size()+C3.size()+C4.size()+C5.size()+C6.size()+C7.size())list.add(C7.get(i));
		}
	} */
	
}
